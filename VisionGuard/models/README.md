# Models

Model weights are intentionally not committed by default.

The YOLO model configured in `config/config.yaml` can be downloaded automatically by Ultralytics when the application first loads it.
